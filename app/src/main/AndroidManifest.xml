<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.filmsapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FilmsApp">
        <activity
            android:name=".ui.Actors_Detail_Activity"
            android:exported="false" />
        <activity
            android:name=".ui.ActorDetailsActivity"
            android:exported="false"
            android:label="@string/title_activity_actor_details"
            android:theme="@style/Theme.FilmsApp.NoActionBar" />
        <activity
            android:name=".ui.MovieDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.Movies_Activity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>